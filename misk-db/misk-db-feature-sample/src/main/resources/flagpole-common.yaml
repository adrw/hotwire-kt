web:
  port: 8080
  idle_timeout: 30000
  http2: true

data_source_clusters:
  flagpole-001:
    writer:
      type: MYSQL
      database: flagpole_db
      username: root
      password: ""
      migrations_resource: classpath:/db-migrations
